<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">
        <%= image_tag('nascenia.png', :class => 'main-logo') %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav nav-tabs navbar-right header-alignment" style="border: none">
          <% if user_signed_in? %>

                <li>
                  <h5 style="margin-top: 13px; margin-right: 15px"> Signed in as: <b><%= current_user.email %></b> </h5>
                </li>
                <% if current_user.super_admin? %>
                    <li class="<%= current_path(manage_admin_and_users_list_admins_path)%>">
                      <%= link_to 'Manage Admin', manage_admin_and_users_list_admins_path %>
                    </li>
                <% end %>
                <li class="<%= current_path(project_assign_path)%>">
                  <%= link_to 'Project Assign', project_assign_path %>
                </li>
                <li class="<%= current_path(projects_path)%>">
                  <%= link_to 'Project', projects_path %>
                </li>
                <li class="<%= current_path(developers_path)%>">
                  <%= link_to 'Developer', developers_path %>
                </li>
                <li>
                  <%= link_to 'Log Out', destroy_user_session_path, :method => :delete %>
                </li>
              </ul>
          <% else %>
              <li>
                <%= link_to 'Log In', new_user_session_path, :method => :get %>
              </li>
          <% end %>
      </ul>



    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>