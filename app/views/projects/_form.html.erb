<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-md-12">
      <div id="addPercentageGroup" class="form-group">
        <%= f.label :name,'Project Name', :class => 'control-label focusOnShow' %>
        <%= f.text_field :name, :class => 'form-control' %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div id="addPercentageGroup" class="form-group">
        <%= f.label :title,'Title of Project', :class => 'control-label focusOnShow' %>
        <%= f.text_field :title, :class => 'form-control' %>
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-12">
      <div id="addPercentageGroup" class="form-group">
        <%= f.label :description,'Description of Project', :class => 'control-label focusOnShow' %>
        <%= f.text_area :description, :class => 'form-control' %>
      </div>
    </div>
  </div>
  <div class="actions clearfix">
    <% unless @project.new_record? %>
      <div class="pull-left">
        <%= link_to 'Show', @project %> | &nbsp;

      </div>
     <%end%>
    <%= link_to 'Back', projects_path %>
    <%= f.submit nil, :class => 'btn btn-primary pull-right' %>
  </div>
<% end %>
