<h1>Project List</h1>
<br/>
<% if can? :create, @projects %>
    <%= link_to 'New Project', new_project_path, :class => 'btn btn-primary' %>
<% end %>
<br/>
<br/>

<div id="projects-container">
  <%= render partial: 'projects/projects_table', locals: { projects: @projects } %>
</div>
